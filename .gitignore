# Raw example source — used only to (re)generate data/ via tools/prepare_data.py.
# Not served by the site, so kept out of the deployed repo.
plots_best100/
phdos/

# OS / Python cruft
.DS_Store
__pycache__/
*.pyc
